DIR="$(cd "$(dirname ${BASH_SOURCE[0]})" ; pwd -P )"
NAME="$(basename ${BASH_SOURCE[0]})"

CXXFLAGS="'-DDXT_DISABLE_LARGE_TESTS=1 -DDEBUG -g3 -ggdb -std=c++17 -Og -ftemplate-backtrace-limit=0'"

source ${DIR}/gcc_base
