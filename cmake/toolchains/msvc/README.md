# CMake Toolchain files for Windows

MIT-licensed code from https://github.com/MarkSchofield/WindowsToolchain

Note that upstream has `.` in the filenames multiple times. This confuses our documentation generation with CMinx
so they were replaced with underscores.

These toolchains are set to chainload by vcpkg in our CMake presets.
