# Documentation / tutorial build dependencies for dune-gdt.
#
# These are the packages needed to render the Sphinx tutorials site and to
# execute the tutorial notebooks via myst-nb (see tutorials/source/conf.py and
# the build_tutorials job in .github/workflows/non_docker_build.yml).
#
# Pinned for reproducible CI docs builds; bumped automatically via Dependabot
# (pip ecosystem, see .github/dependabot.yml). numpy/scipy are intentionally
# left unpinned here so they resolve against the constraints of the freshly
# built dune-gdt/dune-xt wheels that are installed alongside these.
sphinx==7.4.7
myst-nb==1.1.2
furo==2024.8.6
sphinxcontrib-bibtex==2.6.3
python-slugify==8.0.4
pymor==2024.2.0
k3d==2.16.1
vtk==9.3.1
ipywidgets==8.1.5
lxml==5.3.0
xmljson==0.2.1
matplotlib==3.9.2
wurlitzer==3.1.1
